import 'package:flutter/material.dart';

class ShowAllMedicinePage extends StatelessWidget {
  const ShowAllMedicinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('All Medicine show page'),
      ),
    );
  }
}
